query GetTags($siteKey: String!, $fieldSorter: InputFieldSorterInput) {
    admin {
        jahia {
            tagManager(siteKey: $siteKey) {
                tags(fieldSorter: $fieldSorter) {
                    nodes {
                        name
                        occurrences
                    }
                }
            }
        }
    }
}
