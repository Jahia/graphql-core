query GetTaggedContent($siteKey: String!, $tag: String!) {
    admin {
        jahia {
            tagManager(siteKey: $siteKey) {
                taggedContent(tag: $tag) {
                    nodes {
                        uuid
                        path
                    }
                }
            }
        }
    }
}
