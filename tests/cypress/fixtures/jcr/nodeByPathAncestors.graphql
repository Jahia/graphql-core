query($path: String!, $upToPath: String!) {
    jcr(workspace: EDIT) {
        nodeByPath(path: $path) {
            ancestors(upToPath: $upToPath) {
                name
            }
        }
    }
}