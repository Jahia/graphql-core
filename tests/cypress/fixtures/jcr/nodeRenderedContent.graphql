query($path: String!, $view: String, $isEditMode: Boolean = false) {
    jcr {
        nodeByPath(path: $path) {
            renderedContent(view: $view, isEditMode: $isEditMode) {
                output
            }
        }
    }
}
