query($groupName: String!, $site: String){
      admin {
          userGroup {
              group(groupName: $groupName, site: $site) {
                  members {
                      nodes {
                          name
                      }
                  }
              }
          }
      }
 }
