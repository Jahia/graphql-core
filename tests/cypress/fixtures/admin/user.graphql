query(
    $userName: String!
){
  admin {
    userAdmin {
      user(userName: $userName) {
        name
        username
        firstname
        lastname
        email
        language
        organization
        locked
        lastname
      }
    }
  }
}
