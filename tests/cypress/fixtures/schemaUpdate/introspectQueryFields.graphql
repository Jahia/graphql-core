query SchemaUpdateIntrospectQuery {
  __type(name: "Query") {
    fields {
      name
      description
      args {
        name
        type {
          name
          kind
        }
      }
    }
  }
}

