query {
  currentUser {
    name
    firstname
    lastname
    username
    email
    organization
    groupMembership(first: 10) {
       nodes {
         name
       }
    }
    language
    locked
  }
}
